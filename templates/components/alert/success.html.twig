{% macro success(message) %}
    <div class="alert alert-success alert-dismissible fade show rounded-1"
         role="alert">
        <strong>Success!</strong> {{ message | raw }}
        <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
                aria-label="Close"></button>
    </div>
{% endmacro %}