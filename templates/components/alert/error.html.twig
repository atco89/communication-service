{% macro error(message) %}
    <div class="alert alert-danger alert-dismissible fade show rounded-1"
         role="alert">
        <strong>Error!</strong> {{ message | raw }}
        <button
                type="button"
                class="btn-close"
                data-bs-dismiss="alert"
                aria-label="Close"></button>
    </div>
{% endmacro %}